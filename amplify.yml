version: 1
backend:
  phases:
    preBuild:
      commands:
        
npm install
  build:
    commands:
npm start # Si tienes un script de build, si no, usa el comando apropiado
artifacts:
  baseDirectory: /
  files:
'*/'
cache:
  paths:
node_modules/*/
